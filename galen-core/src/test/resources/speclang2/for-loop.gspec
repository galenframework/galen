




= Main section =
    @for [1 - 3] as index1
        @for [5,7,9] as index2
            box-${index1}-${index2} :
                text is "${index1} and ${index2}"

        label-${index1} :
            height 10px

    caption:
        width 50px
