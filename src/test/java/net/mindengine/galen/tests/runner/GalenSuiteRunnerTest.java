package net.mindengine.galen.tests.runner;

import org.testng.annotations.Test;

public class GalenSuiteRunnerTest {


    @Test public void shouldRun_singleTestSuccessfully() {
        
        /*System.setProperty("url", "file://" + getClass().getResource("/html/page-nice.html").getFile());
        System.setProperty("", paramString2)
        
        GalenMain galen = new GalenMain();
        
        galen.execute(new GalenArguments()
            .withAction("test")
            .withPaths(Arrays.asList(""))
            );
            
            */
    }
    
}

